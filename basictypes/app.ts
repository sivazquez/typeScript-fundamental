(
   ()=> {
    //this is a coment 
        const a:number = 10;
        let b:number = 20;
        function printMsg( msg:number ){
            console.log( msg );
        }
        printMsg(a);
        console.log("test ts config");
   }
)()
