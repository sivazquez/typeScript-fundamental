"use strict";
(() => {
    let flash;
    flash = {
        name: "Barry Alen",
        powers: ["Super velocidad", "Viajar en el tiempo"],
        getName() {
            return this.name;
        },
    };
    console.log(flash);
    let multipleTypes;
})();
